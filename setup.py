from setuptools import setup, find_packages

setup(
    name='swarmflow',
    version='0.1.0',
    description='SwarmFlow: A simple multi-agent orchestration runtime',
    author='Anirudh Ramesh',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
) 